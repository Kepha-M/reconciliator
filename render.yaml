services:
  - type: web
    name: fastapi-backend
    env: python
    plan: free
    rootDirectory: backend   # <-- Add this if your main.py is in the backend folder
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
